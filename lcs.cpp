#include <iostream>
#include <string>
using namespace std;

int lcs(string s, string t, int m, int n) {
  if (m == 0 || n == 0) return 0;
  return 1;
}

int main() {
  string s;
  string t;
  cin >> s;
  cin >> t;
  return 0;
}
